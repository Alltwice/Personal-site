---
const { target = 'detail', label = 'SCROLL' } = Astro.props;
---

<a class="scroll-indicator" href={`#${target}`} aria-label={`${label}，进入详细内容`}>
	<i class="ph ph-arrow-down" aria-hidden="true"></i>
	<span>{label}</span>
	<small>VIEW DETAILS</small>
</a>
