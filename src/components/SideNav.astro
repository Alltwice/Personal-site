---
const items = [
	{ href: '#home', label: '首页' },
	{ href: '#articles', label: '文章' },
	{ href: '#projects', label: '项目' },
];
---

<nav class="side-nav" aria-label="首页锚点导航">
	{items.map((item) => <a href={item.href}>{item.label}</a>)}
</nav>
